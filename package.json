{
  "name": "secureshell-pro",
  "private": true,
  "version": "0.4.0",
  "license": "AGPL-3.0-only",
  "description": "A secure desktop SSH workspace for hosts, keys, snippets, terminal sessions, and encrypted LAN sync.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build:deb": "tauri build -b deb",
    "build:appimage": "tauri build -b appimage",
    "build:all": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
